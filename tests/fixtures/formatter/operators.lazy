// sum = a + b
sum = a+b

// diff = a - b
diff = a-b

// prod = a * b
prod = a*b

// complex = a + b * c - d
complex = a+b*c-d

// logical = x && y || z
logical = x&&y||z

// negation = !flag
negation = !flag

// negative = -5
negative = -5
